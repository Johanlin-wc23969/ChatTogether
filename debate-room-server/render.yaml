services:
  - type: web
    name: debate-room-server
    env: go
    plan: free
    buildCommand: go build -o app .
    startCommand: ./app
    envVars:
      - key: PORT
        value: "10000"          # Render 常用端口
      - key: ALLOWED_ORIGINS
        value: "https://chat-together-mu.vercel.app"
